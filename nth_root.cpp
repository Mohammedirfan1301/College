#include <stdio.h>

int main()
{
	
	xk = 1;

	// Get A / N
	prinf("Enter two numbers (A & N): ")
	scanf(// A N

	// LOOP HERE WHILE
	// XK+1 - XK > .000001
	while( (xk+1 - xk) > .000001)
	{
		xk+1 = [ (n - 1) * xk + (A / xk^(n - 1)) ] / n
	}
	


	return 0;
}